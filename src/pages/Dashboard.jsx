const Dashboard = () => {
  return (
    <div>
      {/* Dashboard
      {[...Array(200).keys()].map((el, i) => (
        <p key={i}>{i}</p>
      ))} */}
    </div>
  );
};
export default Dashboard;
